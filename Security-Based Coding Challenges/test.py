# import pxssh

# class Client:
#     def __init__ (self, hostname, username, password):
#         self.hostname = ...
#         self.session = self.connect()

#     def connect(self):
#         try:
#             s = pxssh.pxssh()
#             s.login(self.hostname, ...)
#             return s
#         except:
#             print e

#     def send_command(cmd):
#         self.session.sendline(cmd)
#         self.session.prompt()
#         return self.session.before

# def command_for_botnet(cmd):
#     for client in botnet:
#         output = client.send_command(cmd)

# def addClient(hn, un, p):
#     c = Client(hn, un, p)
#     botnet.append(c)

# botnet = []
# addClient('12.2.1.1', 'aysuh', uedhde)
# command('ls -la')

# rule banker : 
# {
#     meta:
#         description:
#     strings:
#         $a = jdendjend
#         $b = {AA II PP}
#     condition:
#         $s or $b
# }

